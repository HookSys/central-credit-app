apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: consult-storage
  labels:
    run: relato-consult
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 1Gi
