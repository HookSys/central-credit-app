apiVersion: v1
kind: Service
metadata:
  name: serasa-app-svc
  labels:
    app: serasa
    role: service
spec:
  type: NodePort
  externalTrafficPolicy: Cluster
  selector:
    app: serasa
    role: service
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
status:
  loadBalancer: {}
