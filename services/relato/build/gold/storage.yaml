apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gold-storage
  labels:
    run: relato-gold
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 1Gi
