{
  "name": "Central",
  "connector": "rest",
  "options": {
    "headers": {
      "content-type": "application/json"
    }
  },
  "operations": [
    {
      "template": {
        "method": "POST",
        "url": "http://relato-central:6000/email/send",
        "body": {
          "database": "{database:string}",
          "only_payments": "{onlyPayments:string}"
        }
      },
      "functions": {
        "generate": ["database", "onlyPayments"]
      }
    },
    {
      "template": {
        "method": "GET",
        "url": "http://relato-central:6000/consulta/{type:string}"
      },
      "functions": {
        "find": ["type"]
      }
    },
    {
      "template": {
        "headers": {
          "content-type": "text/plain"
        },
        "method": "POST",
        "url": "http://relato-central:6000/relato/saveTitles",
        "body": "{body:string}"
      },
      "functions": {
        "saveTitles": ["body"]
      }
    }
  ]
}
