apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  type: NodePort
  selector:
    run: app
  ports:
    - name: app
      port: 3000
      protocol: TCP
      targetPort: app
    - name: app-db
      port: 27017
      nodePort: 32760
      targetPort: app-db
