apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: central-storage
  labels:
    run: relato-central
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 1Gi
